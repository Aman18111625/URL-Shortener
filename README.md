# URL-Shortener

Problem Statement : 

Design a URL Shortener service which take a valid URL and returned a shortened URL and redirected to the user to previously provided URL along with tracking of click on the URL.